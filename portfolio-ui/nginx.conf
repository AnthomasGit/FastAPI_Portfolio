server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # The magic line that allows client-side React routing to work
        try_files $uri $uri/ /index.html;
    }
}