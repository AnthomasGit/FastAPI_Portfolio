{
  "_comment": "Colour-match post pass (KAN-37). image_node = source still; reference_image_node = approved key frame; color_node = KJNodes ColorMatch; grain_node = ProPostFilmGrain (removed by build_color_match when film grain is off); output_node = SaveImage. No prompt/seed — deterministic image→image pass. UNVALIDATED against a live ComfyUI.",
  "image_node": "1",
  "reference_image_node": "2",
  "color_node": "3",
  "grain_node": "4",
  "output_node": "5"
}
