# Freeze the exact torch stack shipped in yanwk/comfyui-boot:cu130-slim-v2.
# Passed as `pip install -c` while installing the Trellis2 deps so that no
# transitive dependency (open3d, utils3d, rembg, ...) can silently pull a
# different torch build and break the compiled CUDA extensions' ABI.
torch==2.12.0
torchvision==0.27.0
torchaudio==2.11.0
